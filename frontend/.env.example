# Backend base URL. Leave unset for a production build served behind the
# same host as the API (same-origin /api/... paths); set it when the API
# lives on a different origin, e.g. https://api.example.org
VITE_API_BASE_URL=http://127.0.0.1:8000
